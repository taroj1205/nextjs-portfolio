import { faGamepad } from "@fortawesome/free-solid-svg-icons"
import { Icon } from "@yamada-ui/fontawesome"

export const Connect4Icon = ({ ...props }) => (
  <Icon icon={faGamepad} color="blue.600" size={30} {...props} />
)
